import { StyleSheet } from "react-native";

export const signupTextStyle = StyleSheet.create({
    text: {
        fontSize: 16,
        marginTop: 20
    },
    textBold: {
        fontWeight: "bold"
    }
})